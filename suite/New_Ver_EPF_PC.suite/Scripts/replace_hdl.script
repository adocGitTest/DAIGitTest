to handle replace
	put {A:1, B:2, C:3} into myObj
	replace properties {B:"bunny"} of myObj
	put myObj.B into B
	if B="bunny" then
		log "replaceコマンド実行成功"
		result_hdl.Result_Pass "Replaceコマンドで指定テキストを入れ替え","PC"
	else
		log "replaceコマンド実行失敗"
		result_hdl.Result_Fail "Replaceコマンドで指定テキストを入れ替え","PC"
	end if
end replace
